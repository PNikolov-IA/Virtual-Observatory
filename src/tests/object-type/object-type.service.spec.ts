import { ObjectTypesService } from '../../object-type/object-types.service';

describe('ObjectTypesService', () => {
    it('Should create a user.', async () => {

        // const ObjectTypes = {
        //     type: 'galaxy',
        // };
        // const REPOSITORY_MOCK = jest.fn(() => ({
        //     save: jest.fn().mockImplementation((ObjectTypes) => ObjectTypes),
        // }));

        // const SPY = jest.fn(() => ObjectTypes);

        // jest.spyOn(ObjectTypesService, 'getObjectTypes')
        //     .mockImplementation(() => SPY(ObjectTypes));

        // await ObjectTypesService.getObjectTypes();
        // expect(SPY).toHaveBeenCalledTimes(1);
        // expect(SPY).toHaveBeenCalledWith(USER);

    });
});